import { InitialUpperPipe } from './initial-upper.pipe';

describe('InitialUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new InitialUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
